{
  "name": "vscode-copilot-rtl",
  "displayName": "Copilot Chat RTL Support",
  "description": "Adds Right-to-Left (RTL) support and Vazirmatn font styling to the GitHub Copilot Chat panel, for Persian/Farsi, Arabic, Hebrew and other RTL languages.",
  "version": "2.3.0",
  "publisher": "NabiKAZ",
  "license": "GPL-3.0-only",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "rtl",
    "farsi",
    "persian",
    "arabic",
    "copilot",
    "copilot chat",
    "vazirmatn",
    "right-to-left"
  ],
  "icon": "icon.png",
  "repository": {
    "type": "git",
    "url": "https://github.com/NabiKAZ/vscode-copilot-rtl"
  },
  "bugs": {
    "url": "https://github.com/NabiKAZ/vscode-copilot-rtl/issues"
  },
  "homepage": "https://github.com/NabiKAZ/vscode-copilot-rtl#readme",
  "author": "NabiKAZ",
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "copilotRtl.enable",
        "title": "Copilot RTL: Enable (requires reload)"
      },
      {
        "command": "copilotRtl.disable",
        "title": "Copilot RTL: Disable (requires reload; run before uninstalling)"
      },
      {
        "command": "copilotRtl.openMenu",
        "title": "Copilot RTL: Open Menu (direction, font, size…)"
      },
      {
        "command": "copilotRtl.cycleDirection",
        "title": "Copilot RTL: Cycle Direction (auto → RTL → LTR)"
      }
    ],
    "keybindings": [
      {
        "command": "copilotRtl.cycleDirection",
        "key": "ctrl+alt+r",
        "mac": "cmd+alt+r"
      }
    ],
    "configuration": {
      "title": "Copilot RTL",
      "properties": {
        "copilotRtl.autoEnable": {
          "type": "boolean",
          "default": true,
          "description": "Automatically (re)apply the RTL patch to VS Code's workbench on startup."
        },
        "copilotRtl.notifyWhenPatchReverted": {
          "type": "boolean",
          "default": true,
          "description": "Show a notification when the patch is found missing on startup (typically after a VS Code update overwrote it) and 'copilotRtl.autoEnable' is off, offering to re-apply it."
        },
        "copilotRtl.direction": {
          "type": "string",
          "enum": ["auto", "rtl", "ltr"],
          "enumDescriptions": [
            "Detect direction per paragraph/list item/heading from its own text (best for bilingual chats)",
            "Force right-to-left direction in the chat area",
            "Force left-to-right direction in the chat area"
          ],
          "default": "auto",
          "description": "Text direction used in the Copilot Chat area (applies live, no reload needed). The prompt input box always auto-detects direction per line, regardless of this setting."
        },
        "copilotRtl.fontFamily": {
          "type": "string",
          "default": "Vazirmatn",
          "description": "Font family used in the Copilot Chat area (applies live, no reload needed). If the font isn't installed, this falls back to Vazirmatn (using the copy bundled with the extension if Vazirmatn isn't installed either)."
        },
        "copilotRtl.fontSize": {
          "type": "number",
          "default": 13,
          "minimum": 8,
          "maximum": 48,
          "description": "Font size (in pixels) used in the Copilot Chat area (applies live, no reload needed)."
        },
        "copilotRtl.lineHeight": {
          "type": "number",
          "default": 1.6,
          "minimum": 1,
          "maximum": 3,
          "description": "Line height (spacing between lines) used in the Copilot Chat area (applies live, no reload needed)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "echo \"No build step required\""
  },
  "devDependencies": {
    "@vscode/vsce": "^2.24.0"
  }
}
